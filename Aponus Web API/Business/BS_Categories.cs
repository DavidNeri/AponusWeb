using Aponus_Web_API.Data_Transfer_Objects;
using Microsoft.AspNetCore.Mvc;

namespace Aponus_Web_API.Business
{
    public class BS_Categories
    {
        internal IActionResult Actualizar(DTOCategorias Anterior)
        {

            /* try
             {
                 switch (Anterior.IdTipo)
                 {
                     case null:

                         if (Anterior.IdDescripcion != null && Nueva.Descripcion != null)
                         {
                             //ACtualizar Descripcion
                         }

                         break;

                     default:

                         if (Anterior.IdTipo != null && Nueva.DescripcionTipo != null)
                         {
                             //ACtualizar Tipo


                         }

                         break;

                 }
                 return new StatusCodeResult(200);

             }
             catch (Exception)
             {
                 return new StatusCodeResult(500);
             }
            */
            return null;

        }

    }
}
